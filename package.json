{
  "name": "git-remote-sync",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --target=bun-linux-x64 --compile --minify --sourcemap --bytecode index.ts --outfile artifacts/git-remote"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "firebase": "^12.5.0",
    "inquirer": "^12.11.0",
    "pocketbase": "^0.26.3",
    "simple-git": "^3.30.0"
  }
}
