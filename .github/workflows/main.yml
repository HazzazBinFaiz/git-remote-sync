name: Build Bun Project on Release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.21

      - name: Install Dependencies
        run: |
          bun install

      - name: Build Project
        run: |
          bun build --target=bun-linux-x64 --compile --minify --sourcemap --bytecode index.ts --outfile artifacts/git-remote
          bun build --target=bun-windows-x64 --compile --minify --sourcemap --bytecode index.ts --outfile artifacts/git-remote.exe
          
      - name: Upload Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: bun-build-${{ matrix.target }}
          path: |
            artifacts/git-remote
            artifacts/git-remote.exe
